[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
optimizer = true
optimizer_runs = 200
# Pin solc so the build is reproducible for judges.
solc_version = "0.8.24"

[fmt]
line_length = 100
